name: Auto PR Metadata

on:
  pull_request:
    branches: [master, staging]
    types: [opened, synchronize]

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read
    env:
      AI_TITLE_TIMEOUT_MS: '120000'
    outputs:
      pr_number: ${{ steps.create-or-update.outputs.pr_number }}
      pr_title: ${{ steps.create-or-update.outputs.pr_title }}
      pr_body: ${{ steps.create-or-update.outputs.pr_body }}
      needs_label: ${{ steps.create-or-update.outputs.needs_label }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update PR metadata
        id: create-or-update
        timeout-minutes: 10
        uses: actions/github-script@v7
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.OPENAI_BASE_URL }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request;
            const baseBranch = pr.base.ref;

            const { data: commits } = await github.rest.pulls.listCommits({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              per_page: 100,
            });

            const commitList = commits
              .map(c => `- ${c.commit.message.split('\n')[0]} (${c.sha.slice(0, 7)})`)
              .reverse()
              .join('\n');

            const commitSubjects = commits.map(c => c.commit.message.split('\n')[0]);
            const commitMessages = commitSubjects.slice(-20).join('\n');
            const aheadBy = commits.length;

            const titleTimeoutMs = Number(process.env.AI_TITLE_TIMEOUT_MS || 120000);
            const baseUrl = (process.env.OPENAI_BASE_URL || 'https://api.openai.com').replace(/\/$/, '');
            const truncateForLog = (value, maxLength = 400) => {
              const text = String(value ?? '');
              return text.length > maxLength
                ? `${text.slice(0, maxLength)}… [truncated ${text.length - maxLength} chars]`
                : text;
            };
            const parseAiPayload = (raw) => {
              const text = String(raw || '').trim();
              if (!text) {
                throw new Error('Empty AI response body');
              }

              try {
                return JSON.parse(text);
              } catch {
                const ssePayloads = text
                  .split(/\r?\n/)
                  .map(line => line.trim())
                  .filter(line => line.startsWith('data:'))
                  .map(line => line.slice(5).trim())
                  .filter(line => line && line !== '[DONE]');

                for (let index = ssePayloads.length - 1; index >= 0; index -= 1) {
                  try {
                    return JSON.parse(ssePayloads[index]);
                  } catch {
                    // Continue until a valid JSON payload is found.
                  }
                }

                throw new Error(`AI response was not valid JSON: ${text.slice(0, 500)}`);
              }
            };
            const extractAiTitle = (payload) => {
              const choiceContent = payload?.choices?.[0]?.message?.content;
              if (typeof choiceContent === 'string') {
                return choiceContent;
              }

              if (Array.isArray(choiceContent)) {
                return choiceContent
                  .map(item => item?.text || item?.content || '')
                  .join('');
              }

              if (typeof payload?.output_text === 'string') {
                return payload.output_text;
              }

              const responseText = payload?.output
                ?.flatMap(item => item?.content || [])
                ?.map(item => item?.text || '')
                ?.join('');

              return typeof responseText === 'string' ? responseText : '';
            };

            const isOpened = context.payload.action === 'opened';
            let aiTitle = '';
            const hasAiConfig = process.env.OPENAI_API_KEY && process.env.OPENAI_MODEL;

            if (hasAiConfig) {
              try {
                const systemPrompt = isOpened
                  ? 'You are a PR title generator. Given commit messages, generate a PR title following Conventional Commits format: "type(scope): description".\n\nTypes: feat, fix, docs, style, refactor, perf, test, chore, ci, build, revert\nScope: optional, e.g., frontend, backend, api, auth, ui\n\nReturn ONLY the title text, no quotes or explanation.'
                  : `You are a PR title evaluator. Given commit messages and the current PR title, decide if the current title is still appropriate. If it is, return it unchanged. If new commits warrant a better title, suggest an updated one following Conventional Commits format: "type(scope): description".\n\nCurrent PR title: "${pr.title}"\n\nReturn ONLY the title text, no quotes or explanation.`;

                const requestPayload = {
                  model: process.env.OPENAI_MODEL,
                  messages: [
                    {
                      role: 'system',
                      content: systemPrompt,
                    },
                    {
                      role: 'user',
                      content: `Commits:\n${commitMessages}`,
                    },
                  ],
                  temperature: 0.3,
                  stream: false,
                };
                const requestPayloadForLog = {
                  ...requestPayload,
                  messages: requestPayload.messages.map(message => ({
                    ...message,
                    content: truncateForLog(message.content),
                  })),
                };
                console.log('AI title request payload:', JSON.stringify(requestPayloadForLog, null, 2));

                const aiResponse = await fetch(`${baseUrl}/v1/chat/completions`, {
                  method: 'POST',
                  headers: {
                    'Content-Type': 'application/json',
                    'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,
                  },
                  body: JSON.stringify(requestPayload),
                  signal: AbortSignal.timeout(titleTimeoutMs),
                });

                const raw = await aiResponse.text();
                if (!aiResponse.ok) {
                  throw new Error(`API request failed: ${aiResponse.status} ${aiResponse.statusText} - ${raw.slice(0, 500)}`);
                }

                const json = parseAiPayload(raw);
                aiTitle = String(extractAiTitle(json) || '').trim();
                aiTitle = aiTitle.replace(/^['"`]+|['"`]+$/g, '').trim();

                if (!aiTitle) {
                  throw new Error('No title content returned from AI');
                }
              } catch (error) {
                console.log('Failed to generate AI title:', error.message);
              }
            } else {
              console.log('Skipping AI title generation because OPENAI_API_KEY or OPENAI_MODEL is missing.');
            }

            // Only update title if AI generated something meaningfully different from current
            const title = aiTitle && aiTitle.toLowerCase() !== pr.title.toLowerCase() ? aiTitle : pr.title;

            const body = commitList || '_No new commits._';

            await github.rest.pulls.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
              title,
              body,
            });
            console.log(`Updated PR #${pr.number} targeting ${baseBranch}: ${pr.html_url}`);
            core.setOutput('pr_number', pr.number);
            core.setOutput('pr_title', title);
            core.setOutput('pr_body', body);
            core.setOutput('needs_label', 'true');

  call-auto-label:
    needs: create-pr
    if: ${{ needs.create-pr.outputs.needs_label == 'true' }}
    permissions:
      pull-requests: write
      issues: write
      contents: read
    uses: ./.github/workflows/auto-label.yml
    with:
      pr_number: ${{ needs.create-pr.outputs.pr_number }}
      pr_title: ${{ needs.create-pr.outputs.pr_title }}
      pr_body: ${{ needs.create-pr.outputs.pr_body }}
    secrets: inherit
